#include <stdio.h>
 
int main()
{
   int count=0;
  for(count=0;count<5;count++)
  {
      static int value=3;
      printf(" count =%d  \nstatic =%d",count,value);
      value++;
  }

 
    return 0;
}
