// c program to find dublicates in an array 
#include<stdio.h>

int main()
{
    int a[]={1, 2, 3, 4, 2, 7, 8, 8, 3};
    int len=sizeof(a)/sizeof(a[0]);
    for(int i=0;i<len;i++)
    {
        for(int j=i+1;j<len;j++)
        {
            if(a[j]==a[i])
            printf("%d",a[j]);
        }
    }
    return 0;
}
